{
  "name": "@agentic-harness/hooks",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Claude Code SessionEnd capture hook: one session, one vault note.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "goldens": "node tests/update-goldens.mjs",
    "install-hook": "node install.mjs",
    "untagged": "node untagged-sessions.mjs"
  },
  "engines": {
    "node": ">=20.11"
  }
}
